image: golang

stages:
  - build

build:
  stage: build
  script:
   - cd /
   - mv /builds/jonnystoten/mixologist $GOPATH/src/jonnystoten.com/mixologist
   - cd $GOPATH/src/jonnystoten.com/mixologist
   - go build ./...
